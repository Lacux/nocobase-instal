version: '3'
services:
  nocobase:
    image: nocobase/nocobase:latest
    command: nocobase start
    ports:
      - "13000:13000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
